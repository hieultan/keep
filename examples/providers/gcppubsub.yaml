name: gcppubsub-demo
type: gcppubsub
authentication:
  project_id: "$(GCP_PROJECT_ID)"
  subscription: "$(PUBSUB_SUBSCRIPTION)"
  service_account_json: "$(GCP_SERVICE_ACCOUNT_JSON)"  # optional when using Workload Identity
